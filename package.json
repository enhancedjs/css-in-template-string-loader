{
  "name": "vue-template-sass-in-string-loader",
  "private": true,
  "version": "0.0.1",
  "description": "Compile Vue sass templates in template strings at build time.",
  "main": "dist/index.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "clear": "rimraf 'dist/*'",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch",
    "build": "npm run clear && npm run tsc",
    "lint": "tslint -p tsconfig.json -t verbose",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "dependencies": {
    "loader-utils": "^1.2.3",
    "node-sass": "^4.13.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.22",
    "@types/loader-utils": "^1.1.3",
    "@types/node": "^12.12.7",
    "@types/node-sass": "^4.11.0",
    "jest": "^24.9.0",
    "rimraf": "^3.0.0",
    "ts-jest": "^24.1.0",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paleo/vue-template-in-string-loader.git"
  },
  "license": "CC0-1.0",
  "keywords": [
    "vue",
    "sass",
    "template",
    "string"
  ]
}
